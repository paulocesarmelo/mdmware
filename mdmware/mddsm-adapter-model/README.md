## Solving Eclipse classpath issues

You should configure `src` folder if packages name start as `<model-name>.br.inf.ufg.br.<model-name>`.

mddsm-syntheis-model > configure build path > add folder > <model-name>*
